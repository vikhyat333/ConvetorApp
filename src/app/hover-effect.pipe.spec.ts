import { HoverEffectPipe } from './hover-effect.pipe';

describe('HoverEffectPipe', () => {
  it('create an instance', () => {
    const pipe = new HoverEffectPipe();
    expect(pipe).toBeTruthy();
  });
});
